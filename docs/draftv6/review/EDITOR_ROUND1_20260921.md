# 第一轮理论修订交接

状态：R1--R6 已修改，等待独立复审；不表示整稿已达到投稿状态。

## 修改对应

| 意见 | 修改与位置 |
| --- | --- |
| R1 | `action_analysis.tex` B3 改为关系信息与执行更新两个问题，删除正文覆盖公式和残差 MSE 段；`action_analysis_proofs.tex` A-D 保留完整表示风险界、标签及证明，注明不是 C 的前提。 |
| R2 | `representation_analysis.tex` 将 Z 定义为目标命令的完整决策输入，包含学习特征、目标时刻/槽位、所需计划缓存、因果调度记录及确定性读出；显式写出实际规则属于相应可测动作类。RGB 始终保留。 |
| R3 | C 简述理想化最后一步标量纠偏；附录 B-D 给出完整随机任务模型。G、a 均在 [-1,1]，chi 为空，失败概率为 1/2+(a-G)^2/8，近似误差为零，非零关系敏感性 1/2 有效。代理最小化的额外风险为关系 MSE 的 1/8；等概率正负偏移下最优恒定动作失败率 5/8，精确关系决策为 1/2。这些是数学例子，不是实验结果或实际物理模型。 |
| R4 | C 明确比较共用评价分布、参考延续、动作集合/尺度、关系定义/归一化、上下文及执行输入；不假设独立训练编码信息嵌套；共同误差/敏感性上界是比较条件。 |
| R5 | D 明确固定基础预测器和执行规则，最终界只保证其上的反馈增益；语义基础不是 RGB 基准。完整方法需要 RGB/增强表示与无反馈/历史反馈交叉闭环实验。 |
| R6 | D 与附录 B-E 明确历史示例的评价分布只支持最后一次决策，全部当前信息 Fc（含本体、计划和运行记录）均不透露模式；保留有限 GRU 窗口与完整历史的区别。 |

英文理论正文和对应中文注释同步；本轮未翻译方法及实验章节，不将现有混合语言草稿称为可投稿版本。

## 验证

- `bash docs/draftv6/build.sh` 成功生成 `main.pdf`。
- 修改源文件的 `git diff --check` 通过。
- 已检查 PDF 第 4、13 页的定义、公式、实例与附录；交叉引用无未定义告警。
- 仍有既有字体绝对路径、字体回退与 algorithmic.sty 编码告警；未以改科学内容消除这些告警。
- 未运行训练、实机或新实验；未提交、推送、发布。

请优先复核：完整 Z 是否充分又不过度吸收目标动作；简化实例及其适用边界；C 与 D 在相同评价分布及不同执行时刻上的对应。

## 整稿后续计划

1. 理论：本轮已修改，待审核方复审；收到意见后继续修订。
2. 方法：逐项核对最终冻结实现、共享推理线程、缓存/槽位规则、数据划分、标签有效性、潜变量部署模式；标记已实现与待实现，不能用设计计划替代事实。
3. 实验：固定交叉对照与主要终点，建立未填数字的结果表，预先说明样本量、排除条件、人工标签及不确定性报告；不得从旧实验自动推定新协议结果。
4. 图示：补架构、执行时间线及任务布置的明确占位/初始示意；示意不能冒充实机结果或实测性能。
5. 叙述：用同一研究问题对齐摘要、引言、相关工作、讨论；去除未验证的贡献和效果声明。
6. T-RO 与语言：待审核方官方要求核实；先完善中文权威内容，再依据作者已有授权确定英文整稿翻译阶段，最终审阅稿不得杂乱混排。编译成功不等于期刊合规。
7. 收尾：全文符号/引用/表图编号、可追溯证据、空表和待补项审计；每轮通知复审。

## 仍需作者提供或确认

- 最终基础策略、分割器、残差检查点与推理配置清单。
- 各训练模块实际使用的 episode 划分、示范预算与随机种子。
- 新协议下每组逐次任务结果、人工标签、失败视频和执行日志。
- 独立关系真值审核、伪标签误差、可见性有效域及关系尺度。
- 当前信息/历史对照、实际延迟/修正采用、任务与空间分层结果。
- 最终任务完成判定规则、相机配置、试验数量与预先规定排除条件。

上述缺失项进入实验空表或待补清单，不由理论常数或理想化实例填充。
