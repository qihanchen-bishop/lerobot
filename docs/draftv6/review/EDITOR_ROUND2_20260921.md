# 第二轮交审：共同调度、采样口径与文献定位

## 本轮完成

- R3-1：核对 `lerobot_train.py:531`、`train_mask_act_policy.py:4901` 和 `train_slow_fast_residual.py:533`。A采用固定非负归一化权重，排除索引零权重；区分基础可用帧采样和残差缓存样本采样，删除轨迹等权事实断言。
- R3-2：A首次定义历史加入可用运行记录 `mathcal R`，避免与关系算子 rho 同符号。B共同过程明确包含计算/接收转移；附录不再仅要求相同记录字段。
- R3-4：D参考控制器保留反馈计算、接收和缓存更新，仅屏蔽最终动作残差。附录明确同历史下同B，不要求不同闭环轨迹的真实延迟一致；条件于历史的计算转移律仍是显式假设。实验补匹配调度的输出屏蔽组，明确待实现；现有直接关闭反馈组不能冒充该对照。
- R1-4剩余措辞：C仅声明保留RGB输入，不保证编码保留全部外观信息。
- R2：Related Work三个小节已从占位改成中文正文；A2C2的当前信息完整输入与本文需验证的额外历史作用分开。补DP、REMAC、Legato版本条目，SAM2完整18位作者。引言改从执行难以推进出发。
- 未实施的温度、扰动集成和三态标注详细方案移到 `SEGMENTATION_EXTENSION_PLAN.md`，论文附录只保留现状和必要局限，不外移必要证明。
- 页数备忘区分外部补充文本和合规多媒体，补英文摘要200词限制。

## 来源

本地专题阅读记录及归档PDF用于ACT、VIOLA、DBC、AIS、copycat、RAP、RTC、A2C2。
本轮额外核对原始页面：

- https://arxiv.org/abs/2408.00714（SAM2完整作者）
- https://arxiv.org/abs/2303.04137v5（DP扩展版，使用2024版本信息）
- https://arxiv.org/html/2601.20130v1（REMAC）
- https://arxiv.org/html/2602.12978v2（Legato）

REMAC/Legato暂按明确arXiv版本引用，不捏造正式卷页。

## 验证与剩余工作

`bash docs/draftv6/build.sh`成功；PDF17页。无新增未解析引用、Overfull或缺字日志；仍有既有字体fallback和algorithmic编码警告。`git diff --check`通过。

本轮不是整稿完成：架构/时序图、编号空表、实验协议压缩、数据与最终配置集中登记仍待下一轮。结果未填；未运行硬件、训练或修改控制代码；未提交发布。
请优先复核共同调度比较的数学口径及归一化风险写法，再审Related Work的准确性。中文权威流程未改变，原有获准英文理论仅作局部对应修改。
